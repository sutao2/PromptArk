# 变更目录

这里只放**尚未合并**的规格变更。合并后更新 `docs/specs/`，本目录对应文件夹标明已归档或移走。

进行中的变更不是现行真相。Agent 实现时以 `docs/specs/` 为准，除非任务明确说「只做这一份 change」。

模板在 [_template](_template/proposal.md)。已合并的放进 [archive](archive/README.md)。
