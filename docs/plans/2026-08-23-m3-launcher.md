# M3 Launcher Alignment Implementation Plan

> **For agentic workers:** 按任务顺序执行。步骤使用 `- [ ]`。未完成计划不得开始 M4。不要整仓库复制旧前端；只移植独立窗口行为并接新 SQLite。

**Goal:** 把已有 `launcher` 独立窗口接到本机库：全局快捷键、即时搜索、变量填写、复制/粘贴降级、macOS 选中文本。

**Architecture:** 窗口与粘贴/失焦在 Rust（旧 `launcher.rs` / `paste.rs` 的桌面约束）。搜索复用 `list_prompts_in_dir`。变量复用 `renderPrompt`。Vue 只画搜索/填写，不发广场请求。

**Tech Stack:** Vue 3、Vitest、Tauri 2、`tauri-plugin-global-shortcut`、rusqlite、既有 `list_local_prompts`。

## Global Constraints

- 启动器必须是独立窗口，label 保持 `launcher`，不得改成主窗口覆盖层。
- 本地结果不得等待网络；第一期不接广场在线结果。
- 空查询不展示结果列表。
- 业务只走 Rust 命令（ADR 0007）。
- 没有本计划任务之外的应用文件。
- 每一任务结束后跑 `./scripts/docs-check`。
- 旧仓库只读：`prompt-launcher` 的启动器窗口与 Rust 命令，不是 `repository/index.js`。

---

### Task 1: 本地即时搜索

**Files:**

- Create: `desktop/src/LauncherApp.spec.js`
- Modify: `desktop/src/LauncherApp.vue`
- Test: 空查询无列表；输入命中标题

**Interfaces:**

- Consumes: `listLocalPrompts({ query, categoryId })`；`LAUNCHER_RESULT_LIMIT = 20`
- Produces: 非空查询渲染最多 20 条本地结果；空查询不渲染 `role="listbox"`；无 `fetch`

- [x] **Step 1: Write the failing test**

```js
it("hides results on empty query", async () => {
  await createLocalPrompt({ title: "官网生成器", content: "写官网" });
  const w = mount(LauncherApp);
  await flushPromises();
  expect(w.find('[role="listbox"]').exists()).toBe(false);
});

it("lists a local title hit", async () => {
  await createLocalPrompt({ title: "官网生成器", content: "写官网" });
  const w = mount(LauncherApp);
  await flushPromises();
  await w.get("input").setValue("官网");
  await flushPromises();
  expect(w.get('[role="listbox"]').text()).toContain("官网生成器");
});
```

- [x] **Step 2: FAIL**
- [x] **Step 3: `LauncherApp` 调 `listLocalPrompts`；空串不搜；结果切片 20**
- [x] **Step 4: PASS**
- [x] **Step 5: 映射 launcher「输入即搜」「空查询」**
- [x] **Step 6: `./scripts/docs-check`**

---

### Task 2: 键盘选择、填写、直接复制

**Files:**

- Create: `desktop/src/platform/launcherKeyboard.js` + `.test.js`
- Modify: `LauncherApp.vue`（搜索态 / 填写态）
- Test: Enter 进填写；Ctrl/Cmd+Enter 渲染后复制意图

**Interfaces:**

- Consumes: `handleLauncherSearchKey`（逻辑同旧 `launcherKeyboard.js`，不带广场命令）；`extractVariables` / `renderPrompt`
- Produces: 方向键改 `selectedIndex`；Enter 若有变量进入填写态，否则复制并请求隐藏；修饰键+Enter 直接 `copy`

```js
export function handleLauncherSearchKey(event, {
  move = () => {},
  moveTo = () => {},
  rowCount = 0,
  current = () => null,
  activate = () => {},
  close = () => {},
} = {}) {
  if (event.isComposing) return false;
  const moves = { ArrowDown: 1, ArrowUp: -1, PageDown: 5, PageUp: -5 };
  if (event.key in moves) move(moves[event.key]);
  else if (event.key === "Home") moveTo(0);
  else if (event.key === "End") moveTo(rowCount - 1);
  else if (event.key === "Enter") {
    const row = current();
    if (row) activate(row, event.metaKey || event.ctrlKey ? "copy" : "default");
  } else if (event.key === "Escape") close();
  else return false;
  event.preventDefault();
  event.stopPropagation();
  return true;
}
```

- [x] **Step 1:** 测 `Enter` → `default`，`Ctrl+Enter` → `copy`，`Escape` → `close`
- [x] **Step 3:** 填写态复用 `renderPrompt`；未填保留 `{{名称}}`；复制成功后 `recordLocalPromptUse`
- [x] **Step 5:** 映射 launcher「Enter 填写」「直接复制」；variables「同源渲染」改为启动器共用 `renderPrompt`
- [x] **Step 6: `./scripts/docs-check`**

---

### Task 3: 失焦保护期与 Esc 隐藏

**Files:**

- Modify: `desktop/src-tauri/src/commands/launcher.rs`
- Modify: `LauncherApp.vue`（Esc 清空查询再 hide）
- Test: `focus_grace_is_600ms`

**Interfaces:**

- Consumes: 旧 `FOCUS_GRACE = 600ms`
- Produces: `LauncherFocusGuard::mark_shown` / `in_grace_period`；show 时 mark；blur 且不在保护期才 hide；Esc 清空后 hide

```rust
#[test]
fn focus_grace_is_600ms() {
    let guard = LauncherFocusGuard::default();
    assert!(!guard.in_grace_period());
    guard.mark_shown();
    assert!(guard.in_grace_period());
}
```

- [x] **Step 5:** 映射 launcher「关闭」「失焦」
- [x] **Step 6: `./scripts/docs-check`**

---

### Task 4: 全局快捷键登记与冲突

**Files:**

- Create: `desktop/src/platform/shortcut.js` + `.test.js`
- Modify: `SettingsModal.vue` 快捷键页；`settings` 键 `launcher_shortcut`
- Modify: `desktop/package.json`、`src-tauri/Cargo.toml`、`capabilities/default.json`、`lib.rs`（接入 `tauri-plugin-global-shortcut`）
- Test: 冲突返回失败；成功后写入 settings

**Interfaces:**

- Consumes: 默认 `"Control+Space"`（与现有 UI 文案一致，不抢 macOS Spotlight）
- Produces: `registerLauncherShortcut(combo)`：先 unregister，再 register；插件抛错则不写 settings，UI 提示失败；成功 `setLocalSetting("launcher_shortcut", combo)` 并 `toggle_launcher`

```js
it("does not persist when register throws", async () => {
  register.mockRejectedValue(new Error("already registered"));
  await expect(registerLauncherShortcut("Control+Space")).rejects.toThrow(/already registered|冲突/);
});
```

- [x] **Step 5:** 映射 settings「保存快捷键」；launcher「快捷键唤起」
- [x] **Step 6: `./scripts/docs-check`**

---

### Task 5: 粘贴到活动应用与降级

**Files:**

- Create: `desktop/src-tauri/src/commands/paste.rs`（从旧 `paste.rs` 移植桌面粘贴，不移植广场）
- Create: `desktop/src/platform/paste.js` + `.test.js`
- Modify: `lib.rs` 注册 `paste_to_active_app`
- Test: 前端在 invoke 失败时仍保留文本并返回「已复制，未能粘贴」

**Interfaces:**

- Consumes: 剪贴板已由前端写入；`hide_launcher`；macOS `PreviousApplication::restore_previous`
- Produces: `copyThenPaste(text)` → `{ ok: true }` 或 `{ ok: false, message: "已复制，未能粘贴" }`

```js
it("keeps clipboard text when paste command fails", async () => {
  const writeText = vi.fn();
  const invoke = vi.fn().mockRejectedValue(new Error("no accessibility"));
  const result = await copyThenPaste("最终文本", { writeText, invoke });
  expect(writeText).toHaveBeenCalledWith("最终文本");
  expect(result.ok).toBe(false);
  expect(result.message).toMatch(/已复制，未能粘贴/);
});
```

- [x] **Step 5:** 映射 launcher「粘贴成功」「粘贴失败」
- [x] **Step 6: `./scripts/docs-check`**

---

### Task 6: 选中文本仅 macOS

**Files:**

- Modify: `paste.rs` 增加 `capture_selected_text`（非 macOS 返回错误）
- Create: `desktop/src/platform/selectedText.js` + `.test.js`
- Modify: `LauncherApp.vue` 填写态
- Test: 非 macOS 不渲染入口

**Interfaces:**

- Consumes: `supportsSelectedText(platform)` → `platform === "macos"`
- Produces: 填写态按钮「读取选中文本」仅当 `supportsSelectedText`；点击 invoke `capture_selected_text` 填入当前变量

```js
it("hides selected-text on windows", () => {
  expect(supportsSelectedText("windows")).toBe(false);
  expect(supportsSelectedText("macos")).toBe(true);
});
```

- [x] **Step 5:** 映射 launcher「非 macOS」
- [x] **Step 6: `./scripts/docs-check`**

---

### Task 7: 一万条查询基准命令

**Files:**

- Modify: `desktop/src-tauri/src/local_database/tests.rs` 或 `benches` 脚本
- Create: `scripts/launcher-search-bench`（打印耗时，不把超时当 CI 红灯）
- Modify: `docs/specs/launcher/spec.md` 记下命令

**Interfaces:**

- Consumes: `initialize_in_dir`；插入 10000 条后 `list_prompts_in_dir(dir, "官网", None)`
- Produces: 命令输出单次查询毫秒数；规格写明预算 50ms、以本机 `release` 运行为准

```bash
# scripts/launcher-search-bench 最终应能跑：
cd desktop/src-tauri && cargo test --release --locked search_ten_thousand_prompts_bench -- --ignored --nocapture
```

- [x] **Step 5:** 映射「1 万条查询预算有基准命令」
- [x] **Step 6: `./scripts/docs-check`**

---

### Task 8: 收口

**Files:**

- Modify: `docs/plans/status.md`、`milestones/m3.md`
- Create: `docs/plans/done/YYYY-MM-DD-m3-launcher.md`（仅全绿）

- [x] **Step 1:** `npm test` 与 `cargo test --locked` 全绿
- [x] **Step 2:** 启动器与广场均无 fetch；窗口 label 仍是 `launcher`
- [x] **Step 3:** 完成记录 + status
- [x] **Step 4:** `./scripts/docs-check`
- [x] **Step 5:** 不要开始 M4 打包
