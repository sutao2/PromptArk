# 完成记录：M3 启动器对齐

| 字段 | 值 |
|---|---|
| 日期 | 2026-08-23 |
| 里程碑 | M3 |
| 计划 | [2026-08-23-m3-launcher.md](../2026-08-23-m3-launcher.md) |

## 退出标准

- [x] launcher 规格 MUST 均有测试映射或声明基准/手工 QA
- [x] 1 万条查询预算有基准命令 `./scripts/launcher-search-bench`
- [x] 窗口 label 仍是 `launcher`，不是主窗口覆盖层
- [x] 启动器与广场均无 `fetch`
- [x] `cd desktop && npm test`（25 通过）
- [x] `cd desktop/src-tauri && cargo test --locked`（19 通过，1 ignored 基准）
- [x] `./scripts/docs-check` 通过
- [x] 本完成记录已写，status 将 M3 标为完成

## 命令与结果

```text
cd desktop && npm test
Test Files  10 passed (10)
Tests  25 passed (25)

cd desktop/src-tauri && cargo test --locked
19 passed; 0 failed; 1 ignored
（ignored = search_ten_thousand_prompts_bench）

./scripts/docs-check
docs-check 通过（77 个 Markdown 文件）。
```

独立窗口：`tauri.conf.json` 与 `LAUNCHER_LABEL = "launcher"`。全局快捷键默认 `Control+Space`。粘贴失败降级为「已复制，未能粘贴」。选中文本入口仅 macOS。

## 文档

- 更新的规格：launcher、settings、variables
- 更新的 INDEX：是
- status.md 已改为该里程碑完成：是

## 未做 / 下一里程碑带走

- 合集真封面；用户新建小分类；拒绝第三级的自动化
- 1 万条预算以本机 `release` 为准，不作为 CI 红灯
- 不要在无 M4 计划时做分发打包或声称可公开下载
