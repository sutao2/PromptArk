# PromptArk 浏览器工作台

独立浏览器应用，不进桌面安装包。合同见 [web 规格](../docs/specs/web/spec.md)。浏览器使用内存库，**尚未与桌面 SQLite 同步**。

```bash
cd web
npm install
npm test
npm run dev
```

默认 `http://localhost:5175`。不要和桌面抢 `1420`，不要和管理台抢 `5174`。
